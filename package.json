{
  "name": "sheetgrid",
  "private": true,
  "version": "0.1.2",
  "description": "SheetGrid — Excel-class React data grid — virtualized, customizable, responsive.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' build",
    "test": "pnpm -r --filter='./packages/*' test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report",
    "lint": "biome check .",
    "format": "biome format --write .",
    "dev:demo": "pnpm --filter=demo dev",
    "publish:check": "pnpm test && pnpm build && pnpm --filter @sheetgrid/core publish --dry-run --no-git-checks && pnpm --filter @sheetgrid/react publish --dry-run --no-git-checks && pnpm --filter @sheetgrid/tokens publish --dry-run --no-git-checks"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0"
  }
}
